<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="20dp"
    android:background="#FFFFFF"
    android:layout_marginBottom="16dp"
    android:elevation="2dp">

    <!-- Title showing why ConstraintLayout is used -->
    <TextView
        android:id="@+id/tvConstTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="ConstraintLayout (Post Social)"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#9334E6"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/tvConstDesc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Ideal para layouts complexos com hierarquia plana (sem aninhamento excessivo)."
        android:textSize="12sp"
        android:textColor="#5F6368"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvConstTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Avatar circle placeholder -->
    <View
        android:id="@+id/ivAvatar"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="#D2E3FC"
        app:layout_constraintTop_toBottomOf="@id/tvConstDesc"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- User Name -->
    <TextView
        android:id="@+id/tvUserName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Ana Silva"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#202124"
        android:layout_marginStart="12dp"
        app:layout_constraintTop_toTopOf="@id/ivAvatar"
        app:layout_constraintStart_toEndOf="@id/ivAvatar"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Timestamp -->
    <TextView
        android:id="@+id/tvTimestamp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Há 15 minutos • Público"
        android:textSize="12sp"
        android:textColor="#5F6368"
        android:layout_marginStart="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvUserName"
        app:layout_constraintStart_toEndOf="@id/ivAvatar"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Post Body Text -->
    <TextView
        android:id="@+id/tvPostBody"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Explorando as novas funcionalidades do Android Studio e testando ConstraintLayout com hierarquia plana. Incrível como a performance e o posicionamento ficam flexíveis!"
        android:textSize="14sp"
        android:textColor="#202124"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/ivAvatar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Action Buttons Row constrained relatively -->
    <Button
        android:id="@+id/btnLike"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Curtir (24)"
        android:textColor="#1A73E8"
        android:background="?android:attr/selectableItemBackground"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvPostBody"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/btnShare"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Compartilhar"
        android:textColor="#5F6368"
        android:background="?android:attr/selectableItemBackground"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvPostBody"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
